/*
 * Source: GetRegistrosTrnSafCaller.java - Generated by OBCOM SQL Wizard 5.1.266
 * Author: Felipe Gonzalez M (DP S.A.)
 *
 * Copyright (c) OBCOM INGENIERIA S.A. (Chile). All rights reserved.
 *
 * All rights to this product are owned by OBCOM INGENIERIA S.A. and may only be
 * used  under  the  terms of its associated license document. You may NOT copy,
 * modify, sublicense, or distribute this source file or portions of  it  unless
 * previously  authorized in writing by OBCOM INGENIERIA S.A. In any event, this
 * notice and above copyright must always be included verbatim with this file.
 */

package cl.teso.reca.pkgcajaservicestrx;

import java.math.BigDecimal;
import java.sql.CallableStatement;
import java.sql.Connection;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.util.ArrayList;
import javax.sql.DataSource;
import javax.sql.RowSet;

/**
 * Implements a caller of procedure "RECA.PKG_CAJA_SERVICES_TRX.GET_REGISTROS_TRN_SAF".
 * <pre>
 * IN_FOLIO_ENVIO     NUMBER             Input
 * OUT_DATA           REF CURSOR         Output
 * </pre>
 */
public class GetRegistrosTrnSafCaller extends ProcedureCaller
{
    /**
     * Executes procedure "RECA.PKG_CAJA_SERVICES_TRX.GET_REGISTROS_TRN_SAF" using a DataSource.
     */
    public static GetRegistrosTrnSafResult execute(DataSource dataSource, BigDecimal inFolioEnvio)
        throws SQLException
    {
        Connection conn = dataSource.getConnection();
        try {
            return execute(conn, inFolioEnvio);
        } finally {
            conn.close();
        }
    }

    /**
     * Executes procedure "RECA.PKG_CAJA_SERVICES_TRX.GET_REGISTROS_TRN_SAF" using a Connection.
     */
    public static GetRegistrosTrnSafResult execute(Connection conn, BigDecimal inFolioEnvio)
        throws SQLException
    {
        GetRegistrosTrnSafResult result = new GetRegistrosTrnSafResult();
        ArrayList resultSets = new ArrayList();
        if (conn.getMetaData().getURL().startsWith("jdbc:oracle:")) {
            CallableStatement call = conn.prepareCall("{call RECA.PKG_CAJA_SERVICES_TRX.GET_REGISTROS_TRN_SAF(?,?)}");
            try {
                call.setBigDecimal(1, inFolioEnvio);
                call.registerOutParameter(2, ORACLE_CURSOR);
                call.execute();
                resultSets.add(toRowSet((ResultSet)call.getObject(2)));
            } finally {
                call.close();
            }
        } else {
            CallableStatement call = conn.prepareCall("{call RECA.PKG_CAJA_SERVICES_TRX.GET_REGISTROS_TRN_SAF(?)}");
            try {
                call.setBigDecimal(1, inFolioEnvio);
                int updateCount = 0;
                boolean haveRset = call.execute();
                while (haveRset || updateCount != -1) {
                    if (!haveRset)
                        updateCount = call.getUpdateCount();
                    else
                        resultSets.add(toRowSet(call.getResultSet()));
                    haveRset = call.getMoreResults();
                }
            } finally {
                call.close();
            }
        }
        if (resultSets.size() > 0) {
            RowSet[] rowSets = new RowSet[resultSets.size()];
            result.setRowSets((RowSet[]) resultSets.toArray(rowSets));
        }
        return result;
    }
}
